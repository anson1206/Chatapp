services:
  - type: web
    name: chatapp-ws
    env: node
    buildCommand: npm install
    startCommand: node index-ws.js
    envVars:
      - key: MONGO_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: CLIENT_URL
        sync: false
